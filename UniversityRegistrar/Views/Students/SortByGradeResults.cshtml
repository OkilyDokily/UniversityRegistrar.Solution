@model List<UniversityRegistrar.Models.StudentWithGPA>


@if(@Model.Count == 0)
{
<p>No students have this grade point average</p>
}
else
{
@foreach (var item in @Model)
{
  <p>@Html.ActionLink(@item.Student.Name,"Details",new {id = @item.Student.StudentId}) : GPA, @item.GPA</p>
}
}
